{
  "name": "creator-booth",
  "version": "1.0.0",
  "private": true,
  "description": "Creator Booth — a mobile-first chat app that drafts short-form video scripts across 6 content modes, using Gemini (with live Google Search grounding) as primary and Groq as an automatic fallback. Static frontend + Vercel serverless API routes so keys never sit in the browser.",
  "scripts": {
    "dev": "vercel dev"
  }
}
